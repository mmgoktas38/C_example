#include<stdio.h>
	int main()          // Welcome to factorial algorithm
	{
		int x=1;
		int a,n;
		printf("Enter the number you would like the factor to be calculated:");
		scanf("%d",&n);
		
		
		while(n>0)
		{
			x=x*(n);
			n=n-1;
		}
		
		printf("%d",x);
		
		return 0;
	}
